import { sen } from "@/app/fonts/fonts";

export default function ContactHero() {
  return (
<div className="text-center mt-20 md:mt-25">
    <h1 className={`text-sm font-bold  flex justify-center text-[#EB8B3F] ${sen.className}`}>[  Contact  ]</h1>
    <p className={`md:text-6xl text-3xl text-gray-300 ${sen.className} mt-5`}>
     Let’s Work Together.
    </p>
    <p className={`md:text-xl text-lg m-5 text-gray-300 ${sen.className} mt-5`}>Have an idea in mind? Let’s turn it into a polished digital experience together.</p>

    
</div>

  );}
   